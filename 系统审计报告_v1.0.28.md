# TG Search Bot 全系统代理审计评估与优化建议报告
**版本**: v1.0.28 | **审计日期**: 2026-09-04 | **审计人**: AI 高级技术负责人

---

## 一、系统架构总览

```
┌─────────────────────────────────────────────────────────────────────┐
│                        生产环境架构                                  │
├──────────────────────┬──────────────────────┬──────────────────────┤
│  TG Bot (main.py)    │  FastAPI Web (8001)  │  Frontend Demo (/demo)│
│  :8000 polling       │  admin: :8001/admin  │  user: :8001         │
│  python-telegram-bot │  demo:  :8001/demo   │  static: templates/  │
│  Telethon爬虫       │  API:  :8001/api/*   │  CSS/JS内嵌HTML      │
├──────────────────────┼──────────────────────┼──────────────────────┤
│  SQLite + FTS5       │  HD钱包 (TRC20)      │  AI模型池 (DeepSeek) │
│  data/tg_search.db   │  hdwallet库          │  httpx异步客户端      │
└──────────────────────┴──────────────────────┴──────────────────────┘
```

### 关键数据
| 指标 | 数值 |
|------|------|
| server.py 文件大小 | 342 KB（单文件，严重超规模） |
| API 路由总数 | ~60+ 个 |
| 数据库表数 | 20+ 张 |
| Bot 命令数 | 15 个 |
| Demo模块类型 | 8 种 |
| 配置覆盖优先级 | DB > .env > 代码默认值 |

---

## 二、安全审计（高危/中危/低危）

### 🔴 高危（必须立即修复）

#### H1. 管理员密码硬编码 + 无暴力破解防护
**位置**: `server.py:74-76, 1300`
```python
# 默认密码硬编码，且无登录失败计数/锁定
ADMIN_CREDENTIALS = {"username": "admin", "password": "demo123456"}
# 登录验证完全明文比较，无任何频率限制
if u != cred["username"] or pw != cred["password"]:
```
**风险**: 攻击者可通过暴力破解或字典攻击获取管理员权限，控制全部系统功能（含HD钱包助记词、Bot Token、AI Key等）。
**修复建议**:
```python
import hashlib, time
LOGIN_ATTEMPTS = {}  # key: ip, value: {"count": N, "locked_until": timestamp}

MAX_ATTEMPTS = 5
LOCKOUT_DURATION = 900  # 15分钟

# 登录前检查锁定
ip = request.client.host if request.client else "unknown"
attempt = LOGIN_ATTEMPTS.get(ip, {"count": 0, "locked_until": 0})
if attempt["locked_until"] > time.time():
    return JSONResponse({"ok": False, "error": "账户已临时锁定，请15分钟后重试"}, status_code=423)

# 验证后重置计数
if valid:
    LOGIN_ATTEMPTS.pop(ip, None)
else:
    attempt["count"] += 1
    if attempt["count"] >= MAX_ATTEMPTS:
        attempt["locked_until"] = time.time() + LOCKOUT_DURATION
    LOGIN_ATTEMPTS[ip] = attempt
```

#### H2. HD钱包私钥加密算法不安全（XOR）
**位置**: `app/wallet/wallet_manager.py:75-85`
```python
def _encrypt_private_key(priv_hex: str) -> str:
    # 简化加密：先base64再hash前缀标记，生产请换 cryptography.Fernet
    masked = bytes(b ^ key[i % len(key)] for i, b in enumerate(data))
    return "ENCv1:" + base64.b64encode(masked).decode()
```
**风险**: XOR密码是经典弱加密，已知明文攻击即可恢复私钥。一旦数据库被拖库，所有用户TRC20私钥可被还原。
**修复建议**:
```python
from cryptography.fernet import Fernet
import base64

def _encrypt_private_key(priv_hex: str) -> str:
    if not priv_hex: return ""
    key = Fernet.generate_key()  # 或从 CRYPTO_SECRET 派生
    f = Fernet(key)
    return "ENCv2:" + f.encrypt(priv_hex.encode()).decode()

def _decrypt_private_key(encrypted: str) -> str:
    if not encrypted or not encrypted.startswith("ENCv2:"):
        return encrypted  # 兼容旧数据
    key = Fernet.generate_key()  # 从 CRYPTO_SECRET 派生
    f = Fernet(key)
    return f.decrypt(encrypted[6:]).decode()
```

#### H3. /api/diagnostic 无需认证暴露系统信息
**位置**: `server.py:2223`
```python
@app.get("/api/diagnostic")
async def diagnostic_api():
    """系统诊断 API - 返回完整的系统诊断信息（无需登录）"""
```
**风险**: 暴露服务器路径（`/www/wwwroot/tg-search-bot/`）、服务状态、端口、Git信息、数据库表结构，为攻击者提供精确攻击地图。
**修复建议**: 添加登录验证或限制仅内网IP访问。

#### H4. subprocess shell=True 潜在命令注入
**位置**: `server.py:2106`
```python
result = subprocess.run(cmd, shell=True, capture_output=True, text=True, timeout=timeout)
```
**风险**: 若 `cmd` 参数来自用户输入（即使当前看似内部调用），shell=True 可导致命令注入。
**修复建议**: 改用列表形式调用，避免 shell=True：
```python
result = subprocess.run(cmd.split() if isinstance(cmd, str) else cmd,
                       capture_output=True, text=True, timeout=timeout)
```

---

### 🟡 中危（建议优先修复）

#### M1. Session存储于内存，服务重启即失效
**位置**: `server.py:78`
```python
ADMIN_SESSIONS = {}  # 纯内存字典，进程重启丢失所有session
```
**影响**: 用户每次访问需重新登录；服务崩溃重启后所有管理员需重新登录。
**修复建议**: 将 session 持久化到 SQLite 或 Redis：
```python
# 方案A：SQLite持久化
async def _save_session_to_db(session_id, username):
    async with get_db() as db:
        await db.execute("""
            INSERT OR REPLACE INTO admin_sessions (session_id, username, expire_at)
            VALUES (?, ?, ?)
        """, (session_id, username, time.time() + 86400))

# 方案B：轻量级Redis（推荐）
import redis
_redis = redis.Redis.from_url(os.environ.get("REDIS_URL", "redis://localhost:6379/0"))
```

#### M2. 无CORS配置，API可从任意域名调用
**位置**: `server.py`（无CORS中间件）
**风险**: 恶意网站可构造跨域请求调用 `/api/bot/command`（虽为POST且需参数，但缺乏防护）。
**修复建议**:
```python
from fastapi.middleware.cors import CORSMiddleware
app.add_middleware(
    CORSMiddleware,
    allow_origins=["https://jsou.tgjsbot.kdns.fr"],  # 限制特定域名
    allow_methods=["GET", "POST"],
    allow_headers=["Content-Type"],
)
```

#### M3. 无请求频率限制（Rate Limiting）
**位置**: 所有API路由均无速率限制
**风险**: 攻击者可无限次调用 `/api/bot/command` 进行关键词搜索、充值模拟等操作；`/api/admin/login` 无暴力破解防护（见H1）。
**修复建议**: 使用 `slowapi` 或手动IP级限流：
```python
from slowapi import Limiter
from slowapi.util import get_remote_address
limiter = Limiter(key_func=get_remote_address)
app.state.limiter = limiter
app.add_error_handler(RateLimitExceeded, lambda r, h: JSONResponse({"ok": False, "error": "请求过于频繁"}, status_code=429))
```

#### M4. Bot Token 在启动日志明文输出
**位置**: `main.py:109`
```python
logger.info(f"Bot Token: {'已配置 ✓' if Config.BOT_TOKEN else '未配置（将从数据库加载）'}")
```
**风险**: 日志文件（stdout.log/stderr.log）包含完整 Bot Token，任何人读取日志文件即可控制Bot。
**修复建议**:
```python
token_masked = Config.BOT_TOKEN[:6] + "****" + Config.BOT_TOKEN[-4:] if Config.BOT_TOKEN else "未配置"
logger.info(f"Bot Token: {token_masked}")
```

#### M5. 演示用户ID为固定常量，无真实性隔离
**位置**: `server.py:64-68`
```python
DEMO_USERS = [
    {"tg_user_id": 10000001, ...},
    {"tg_user_id": 10000002, ...},
    {"tg_user_id": 10000003, ...},
]
```
**风险**: 演示用户ID与真实用户ID无隔离，若生产环境真实用户注册了这些ID，会引发数据混乱。
**修复建议**: 使用高位ID范围（如 90000001+）或添加 `is_demo` 标记字段。

---

### 🟢 低危（建议优化）

#### L1. 管理后台无操作审计日志
**影响**: 管理员删除广告、修改设置、操作数据库等关键行为无日志记录，无法追溯。
**建议**: 在关键API操作前记录审计日志（操作人、IP、动作、时间）。

#### L2. 备份文件命名包含版本号而非哈希
**影响**: `backup_{version}_{timestamp}.db` 版本号为语义版本，非git commit hash，恢复时可能恢复错误版本。
**建议**: 同时记录 git commit hash：`backup_{git_hash}_{timestamp}.db`

#### L3. 日志重复配置
**位置**: `main.py:51-75`
```python
logger.remove()
logger.add(sys.stderr, ...)      # 第一次
logger.add(f"{Config.LOG_DIR}/bot_{{time}}.log", ...)  # 第一次
logger.remove()                  # 第二次remove会报错（已不存在）
logger.add(sys.stderr, ...)      # 重复添加
logger.add(f"{Config.LOG_DIR}/bot_{{time}}.log", ...)  # 重复添加
```
**影响**: 每条日志会被记录两次（重复输出到stderr和日志文件）。
**建议**: 删除重复的 `logger.add` 调用。

---

## 三、逻辑与架构分析

### 3.1 server.py 单文件架构问题（严重）

**现状**: server.py 342KB，包含：
- 所有API路由（60+个）
- 内嵌HTML/CSS/JS（admin页面 + demo页面 + bot页面）
- 登录认证逻辑
- 诊断工具

**问题**:
| 问题 | 影响 |
|------|------|
| 无法并行开发 | 多人协作必产生merge冲突 |
| 无法单元测试 | 路由和业务逻辑完全耦合 |
| 内存占用高 | 单文件加载慢，冷启动时间长 |
| 无法独立部署 | 任何修改需重启整个服务 |

**建议架构重构**:
```
server.py (30KB)          ← 仅路由注册 + 启动逻辑
├── app/
│   ├── api/
│   │   ├── admin.py      ← 所有管理后台API
│   │   ├── bot.py        ← /api/bot/* 路由
│   │   ├── demo.py       ← /demo /api/demo/* 路由
│   │   └── diagnostic.py ← 诊断API
│   ├── templates/
│   │   ├── admin.html    ← 管理后台HTML
│   │   └── demo.html     ← 演示页HTML（已独立）
│   └── static/
│       ├── css/
│       └── js/
```

### 3.2 API调用链设计（优秀）

```
Telegram Bot (handlers.py)
    ↓ POST /api/bot/command
FastAPI Server (server.py)
    ├── 业务逻辑
    ├── SQLite DB
    ├── Telegram API (httpx)
    └── TronGrid API
```

**优点**: Bot和Web共用同一API层，逻辑不重复，维护成本低。

### 3.3 数据库设计评估

**优点**:
- FTS5全文检索 + jieba中文分词，搜索质量较高
- FTS5触发器自动维护索引，数据一致性有保障
- 迁移脚本支持表结构自动升级

**问题**:
| 问题 | 建议 |
|------|------|
| 无数据库连接池配置 | aiosqlite默认单连接，并发高时排队 |
| 无定期VACUUM | SQLite长期运行后文件膨胀 |
| 无索引统计分析 | messages表无创建时间索引，按日期查询慢 |
| 充值订单无索引 | `WHERE status='pending' AND updated_at < ?` 无索引 |

**修复建议**:
```sql
-- 充值查询优化
CREATE INDEX IF NOT EXISTS idx_recharge_status_time ON recharge_orders(status, updated_at);
-- 消息按时间查询
CREATE INDEX IF NOT EXISTS idx_messages_created_at ON messages(created_at);
-- 定期VACUUM（每周一次）
VACUUM;
```

### 3.4 爬虫系统设计评估

**优点**:
- 多账户轮询池，洪水等待自动处理
- 每日加入上限/间隔控制
- 代理支持（socks5/http）

**问题**:
| 问题 | 风险 |
|------|------|
| 无账户健康度评分 | 无法区分优质/劣质账号，调度盲目 |
| 无IP轮换策略 | 同一IP多次加入易被封 |
| 爬虫日志未分离 | 爬虫日志混入主日志，排查困难 |

### 3.5 钱包系统设计评估

**优点**:
- HD钱包BIP44派生，用户地址唯一且可恢复
- TronGrid链上扫描，充值确认可靠
- 72小时超时自动过期，资金安全

**问题**:
| 问题 | 风险 |
|------|------|
| 私钥XOR加密 | 数据库泄露即私钥泄露（见H2） |
| 无资金归集告警 | 归集操作无日志通知 |
| 无多签名支持 | 单私钥控制全部资金 |
| Mock地址模式无保护 | 未配置助记词时生成假地址，用户以为真实充值 |

---

## 四、性能与稳定性分析

### 4.1 当前性能瓶颈

| 瓶颈 | 位置 | 影响 |
|------|------|------|
| 同步subprocess调用 | `_run_cmd()` 多处 | 阻塞事件循环 |
| SQLite WAL模式未显式启用 | database.py | 并发写入慢 |
| 无API响应缓存 | 所有查询接口 | 重复查询DB |
| Bot命令全走HTTP | handlers.py → server.py | 额外网络开销 |

### 4.2 并发安全评估

| 组件 | 安全性 | 问题 |
|------|--------|------|
| ADMIN_SESSIONS | ⚠️ 非线程安全 | 高并发下可能竞态 |
| _HDWALLET_INSTANCE | ✅ 懒加载安全 | 无锁但只写一次 |
| account_pool | ⚠️ 部分竞态 | 状态修改未加锁 |
| wallet_manager | ✅ 每次创建新db连接 | 安全但性能差 |

### 4.3 建议性能优化

```python
# 1. 启用SQLite WAL模式
async def _init_db():
    async with get_db() as db:
        await db.execute("PRAGMA journal_mode=WAL")
        await db.execute("PRAGMA synchronous=NORMAL")
        await db.execute("PRAGMA cache_size=-64000")  # 64MB缓存

# 2. 添加简单LRU缓存
from functools import lru_cache
import aiocache

@aiocache.cached(ttl=60)
async def get_hot_keywords_cached():
    return await ad_manager.get_hot_keywords_by_category()

# 3. 异步化subprocess
async def _run_cmd_async(cmd: str, timeout: int = 10) -> dict:
    loop = asyncio.get_event_loop()
    return await loop.run_in_executor(None, lambda: subprocess.run(
        cmd if isinstance(cmd, list) else cmd.split(),
        capture_output=True, text=True, timeout=timeout
    ))
```

---

## 五、代码质量分析

### 5.1 代码规范问题

| 问题 | 示例 | 建议 |
|------|------|------|
| 全局变量过多 | `ADMIN_SESSIONS`, `MONITOR_SHARE_LINKS`, `_ai_health_status` | 使用类封装 |
| 硬编码路径 | `/www/wwwroot/tg-search-bot/logs/` | 使用 `Config.LOG_DIR` |
| 魔法数字 | `86400` (1天), `15.0` (超时) | 定义为常量 |
| 异常吞没 | `except Exception: pass` | 至少记录日志 |
| 重复代码 | logger.add 重复4次 | 提取为函数 |

### 5.2 错误处理问题

```python
# 当前：很多API的错误处理过于简单
except Exception as e:
    return JSONResponse({"ok": False, "error": str(e)[:200]})

# 建议：区分异常类型，返回结构化错误
except ValidationError as e:
    return JSONResponse({"ok": False, "error": "参数错误", "details": str(e)}, status_code=400)
except DatabaseError as e:
    logger.error(f"DB error: {e}")
    return JSONResponse({"ok": False, "error": "数据库操作失败"}, status_code=503)
except Exception as e:
    logger.exception(f"Unexpected error: {e}")
    return JSONResponse({"ok": False, "error": "系统内部错误"}, status_code=500)
```

### 5.3 Demo页面问题

| 问题 | 影响 |
|------|------|
| CSS/JS全内嵌HTML | 无法缓存，每次加载重复传输 |
| 无错误边界 | 单个模块失败影响整体 |
| localStorage依赖 | 无痕模式布局丢失 |

---

## 六、优化建议优先级

### P0 — 立即修复（安全/资金风险）
1. **H2**: 私钥加密升级为Fernet/AES-GCM
2. **H1**: 登录添加暴力破解防护（IP级锁定）
3. **M4**: 日志中隐藏Bot Token

### P1 — 本周内修复（重要功能）
4. **M3**: 添加Rate Limiting
5. **M1**: Session持久化到数据库
6. **M2**: 添加CORS配置
7. **H3**: diagnostic API添加认证或内网限制

### P2 — 本月内完成（体验优化）
8. **L2**: 备份文件名改用git commit hash
9. **L3**: 修复重复日志配置
10. **架构**: server.py 拆分模块化
11. **数据库**: 添加缺失索引

### P3 — 长期规划（商业扩展）
12. **高可用**: SQLite → PostgreSQL（支持多实例）
13. **监控**: 添加Prometheus指标端点
14. **灰度**: 支持A/B测试不同Bot页面版本
15. **CDN**: 静态资源走CDN缓存

---

## 七、系统稳定性评估

| 维度 | 评分 | 说明 |
|------|------|------|
| 可用性 | 7/10 | 无健康检查自动化，手动重启 |
| 可靠性 | 6/10 | 无熔断/降级，外部API故障会阻塞 |
| 可扩展性 | 5/10 | 单节点SQLite，用户量>1万需重构 |
| 可维护性 | 6/10 | server.py过大，无单元测试 |
| 安全性 | 5/10 | 多个高危漏洞待修复 |
| 性能 | 6/10 | SQLite并发弱，无缓存层 |

**综合评分**: **5.8/10** — 当前版本适合小规模运营（<1000用户），大规模商业化前需完成P0+P1优化。

---

## 八、关键决策建议

### 立即执行
1. 升级私钥加密为 Fernet（wallet_manager.py）
2. 登录接口添加 IP 级暴力破解防护
3. 日志隐藏敏感信息
4. diagnostic API 添加认证

### 短期规划（1个月内）
1. server.py 拆分（路由/模板/逻辑分离）
2. 添加 Rate Limiting
3. Session 持久化
4. 数据库索引优化
5. 添加 Prometheus 监控

### 长期规划（3个月内）
1. 数据库迁移到 PostgreSQL
2. 添加单元测试（pytest）
3. 实现 CI/CD 自动化测试
4. 添加 API 文档（OpenAPI/Swagger）

---

*报告生成完毕。建议按优先级 P0→P1→P2→P3 逐步修复。*
