# TG Search Bot 宝塔面板部署教程 v1.0.12

> 适用系统：CentOS 7+ / Ubuntu 20.04+ / Debian 11+
> 更新日期：2026-09-02

***

## 目录

1. [准备工作](#1-准备工作)
2. [安装宝塔面板](#2-安装宝塔面板)
3. [上传源码到服务器](#3-上传源码到服务器)
4. [一键部署命令](#4-一键部署命令)
5. [手工部署步骤](#5-手工部署步骤)
6. [配置 .env 文件](#6-配置-env-文件)
7. [首次登录采集账号](#7-首次登录采集账号)
8. [Nginx 反向代理 + HTTPS](#8-nginx-反向代理--https)
9. [后台首次配置](#9-后台首次配置)
10. [常见运维命令](#10-常见运维命令)
11. [排错手册](#11-排错手册)
12. [安全红线](#12-安全红线)

***

## 1. 准备工作

### 1.1 服务器要求

* CPU：≥ 1 核（推荐 2 核+）

* 内存：≥ 1GB（推荐 2GB+）

* 硬盘：≥ 20GB SSD

* 网络：能访问 Telegram 服务器

  * 中国大陆服务器**必须使用境外代理**，建议选海外服务器（香港、新加坡、日本、美国）

### 1.2 您需要准备的材料

| 材料                     | 说明            | 获取方式                                                           |
| ---------------------- | ------------- | -------------------------------------------------------------- |
| **TG Bot Token**       | 机器人令牌         | 找 @BotFather 发送 `/newbot`，按提示创建                                |
| **管理员 TG ID**          | 管理员账号         | 把消息转发给 @userinfobot，读取 `from.id`                               |
| **Telethon api\_id**   | 多组逗号分隔        | <https://my.telegram.org> → API tools → Create new application |
| **Telethon api\_hash** | 多组逗号分隔        | 同上，与 api\_id 一一对应                                              |
| **采集手机号**              | 至少 1 个        | 带区号的真实 TG 注册手机号（如 +8613800138000）                              |
| **HD 钱包助记词**           | 12 或 24 个英文单词 | TronLink 创建钱包时自动生成，**只能手写在 .env**                              |
| **TronGrid API Key**   | 可选            | 免费申请 <https://www.trongrid.io（用于> USDT 充值对账）                   |
| **域名**                 | 可选            | 如 admin.yourdomain.com                                         |

***

## 2. 安装宝塔面板

**SSH 用 root 登录服务器，按系统执行对应命令：**

```bash
# CentOS
yum install -y wget && wget -O install.sh https://download.bt.cn/install/install_6.0.sh && sh install.sh ed8484bec

# Ubuntu / Debian
wget -O install.sh https://download.bt.cn/install/install-ubuntu_6.0.sh && sudo bash install.sh ed8484bec
```

安装完成后终端会输出：

* 外网面板地址（如 `http://IP:8888/xxxxxx`）

* username / password

浏览器登录宝塔面板 → **软件商店**安装以下组件：

* **Nginx 1.24+**（反向代理 + HTTPS 用）

宝塔防火墙放行端口：

* 宝塔 → **安全** → 放行：`80`、`443`、`8001`（Nginx 配完后可关闭 8001）

* 云服务商安全组同步放行：`80`、`443`、`8001`

***

## 3. 上传源码到服务器

### 方式 A：宝塔文件管理器（适合小白）

1. 本机解压 `tg-search-bot-v1.0.12-YYYYMMDD.zip`
2. 宝塔 → **文件** → 进入 `/www/wwwroot/`
3. 新建目录 `tg-search-bot`
4. 进入目录，把解压后的所有文件拖拽上传

   * 注意：`.env.example`、`deploy/` 目录都要上传

### 方式 B：命令行上传（推荐，不丢权限）

```bash
# 本机执行（Windows PowerShell / Mac / Linux）
scp tg-search-bot-v1.0.12-*.zip root@您的服务器IP:/tmp/

# SSH 到服务器执行
mkdir -p /www/wwwroot/tg-search-bot
cd /tmp && unzip tg-search-bot-v1.0.12-*.zip
cp -r tg-search-bot/* /www/wwwroot/tg-search-bot/
chown -R root:root /www/wwwroot/tg-search-bot
chmod +x /www/wwwroot/tg-search-bot/deploy/*.sh
chmod +x /www/wwwroot/tg-search-bot/deploy/scripts/*.sh
```

***

## 4. 一键部署命令

> 源码上传完成后，SSH 到服务器执行以下命令即可。

### 方式一：全自动脚本（推荐新手）

```bash
cd /www/wwwroot/tg-search-bot
bash deploy/oneclick.sh
```

脚本会自动检测宝塔环境并执行 `deploy/baota_install.sh`，完成以下操作：

* ✅ 检测并安装 Python 3.11（如缺失）

* ✅ 创建虚拟环境 venv

* ✅ pip 安装 requirements.txt（清华镜像）

* ✅ 验证核心依赖包

* ✅ 创建 Systemd 服务（Bot + Admin 双服务）

* ✅ 开机自启配置

* ✅ 防火墙端口检查

* ⚠️ Nginx 反向代理需手动配置（见第 8 步）

### 方式二：远程下载部署（无需上传文件）

```bash
# 从远程 URL 自动下载并部署
cd /www/wwwroot
mkdir -p tg-search-bot && cd tg-search-bot
bash <(curl -sSL https://您的服务器/tg-search-bot-v1.0.12.zip) 2>/dev/null || \
curl -sSL https://您的服务器/tg-search-bot-v1.0.12.zip -o /tmp/bot.zip && \
unzip -qo /tmp/bot.zip -d . && \
rm /tmp/bot.zip && bash deploy/oneclick.sh
```

### 方式三：手动指定脚本模式

```bash
cd /www/wwwroot/tg-search-bot
# 强制宝塔模式
bash deploy/oneclick.sh --baota
# 强制标准模式
bash deploy/oneclick.sh --standalone
```

***

## 5. 手工部署步骤

### 5.1 安装系统依赖

```bash
# CentOS
yum install -y git gcc gcc-c++ make libffi-devel \
    zlib-devel bzip2-devel openssl-devel ncurses-devel \
    sqlite-devel readline-devel tk-devel gdbm-devel \
    xz-devel wget curl

# Ubuntu / Debian
apt update && apt install -y git build-essential \
    libffi-dev zlib1g-dev libbz2-dev libssl-dev \
    libncurses5-dev libsqlite3-dev libreadline-dev \
    libgdbm-dev liblzma-dev wget curl
```

### 5.2 安装 Python 3.11

```bash
python3 --version
# 若已是 3.10+ 可跳过；否则：
cd /tmp
wget https://www.python.org/ftp/python/3.11.9/Python-3.11.9.tgz
tar -xzf Python-3.11.9.tgz
cd Python-3.11.9
./configure --prefix=/usr/local/python3.11 --enable-optimizations --with-ssl
make -j$(nproc) && make altinstall
ln -sf /usr/local/python3.11/bin/python3.11 /usr/local/bin/python3.11
python3.11 --version  # 确认输出 3.11.9
```

### 5.3 创建虚拟环境 + 安装依赖

```bash
cd /www/wwwroot/tg-search-bot
python3.11 -m venv venv
source venv/bin/activate
pip install --upgrade pip setuptools wheel -i https://pypi.tuna.tsinghua.edu.cn/simple
pip install -r requirements.txt -i https://pypi.tuna.tsinghua.edu.cn/simple
python -c "import fastapi, telethon, aiosqlite, jieba, loguru, dotenv; print('✅ 依赖安装完成')"
```

### 5.4 创建必要目录

```bash
mkdir -p data/sessions data/backups logs
```

***

## 6. 配置 .env 文件

### 6.1 生成安全密钥（一键生成）

```bash
cd /www/wwwroot/tg-search-bot
source venv/bin/activate
bash deploy/scripts/generate_secrets.sh
```

脚本会交互式询问是否同时生成 TRON HD 钱包助记词。建议先不生成助记词，由您自己从 TronLink 复制。

### 6.2 填入配置

```bash
cd /www/wwwroot/tg-search-bot
vim .env
```

**最少必填项（3 项）：**

```bash
# 机器人令牌（必填）
TG_BOT_TOKEN=123456789:ABC-DEF1234ghIkl-zyx57W2v1u123ew11

# HD钱包助记词（必填，只能手写！）
HD_WALLET_MNEMONIC=word1 word2 word3 word4 word5 word6 word7 word8 word9 word10 word11 word12

# 管理员TG ID（必填）
ADMIN_TG_IDS=123456789
```

**采集账号池（必填，数量一致）：**

```bash
# 格式：多组逗号分隔，数量必须一致
TELETHON_API_IDS=12345678,87654321
TELETHON_API_HASHS=abc123...,xyz789...
TELETHON_PHONES=+8613800138000,+8613900139000
```

**其余配置（可在后台填写，留空即可）：**

```bash
# 搜索参数
SEARCH_RESULT_LIMIT=20
MAX_CHANNELS_PER_ACCOUNT=450
JOIN_INTERVAL_SECONDS=45
MAX_JOIN_PER_DAY=40

# 钱包参数
TRONGRID_API_KEY=
RECHARGE_CONFIRMATIONS=12
MIN_RECHARGE_USDT=0.5

# 路径参数
SESSION_DIR=./data/sessions
DB_PATH=./data/tg_search.db
```

> ⚠️ **重要**：写完 `.env` 后立即执行安全操作：
>
> ```bash
> chmod 600 .env              # 文件权限设为仅 root 可读写
> history -c && rm -f ~/.bash_history   # 清除终端历史
> exit  # 退出 SSH 重登，防止终端 scrollback 泄露信息
> ```

***

## 7. 首次登录采集账号

> ⚠️ **必须在启动 systemd 服务之前完成！**
> 因为登录需要您交互式输入短信验证码和 2FA 密码。

```bash
cd /www/wwwroot/tg-search-bot
source venv/bin/activate
bash deploy/scripts/login_accounts.sh
```

脚本会逐组账号执行：

1. 按 Enter 发送短信验证码
2. 输入 5 位数字短信验证码
3. 若开启 2FA，输入二级密码
4. 显示 "会话写入成功: data/sessions/account\_N.session" 即完成

验证 session 文件：

```bash
ls -la data/sessions/
# 应看到 account_1.session, account_2.session ...
```

***

## 8. Nginx 反向代理 + HTTPS

### 8.1 宝塔面板配置反向代理

1. 宝塔 → **网站** → 点击您的站点 → **反向代理** → **添加反向代理**
2. 填写：

   * 代理名称：`tg_search_admin`

   * 目标 URL：`http://127.0.0.1:8001`

   * 发送域名：`$host`
3. 自定义标头（重要！）：

   ```
   Host $host
   X-Real-IP $remote_addr
   X-Forwarded-For $proxy_add_x_forwarded_for
   X-Forwarded-Proto $scheme
   Upgrade $http_upgrade
   Connection "upgrade"
   ```
4. 超时设置：全部填 `300` 秒
5. 保存后**重载 Nginx**：宝塔 → 服务 → Nginx → **重载配置**

### 8.2 申请 SSL 证书（Let's Encrypt）

1. 宝塔 → **网站** → 点击您的站点 → **SSL**
2. 选择 **Let's Encrypt** 申请证书
3. 勾选域名 → 申请 → 申请成功后开启 **强制 HTTPS**

### 8.3 关闭 8001 外网端口（安全加固）

```bash
# 宝塔 → 安全 → 删除 8001 放行规则
# 云服务商安全组 → 也删除 8001 规则
```

***

## 9. 后台首次配置

### 9.1 访问后台

浏览器打开：

* 无域名：`http://服务器IP:8001/admin`

* 有域名：`https://admin.yourdomain.com/admin`

默认账号：`admin` / `demo123456`（登录后立即修改）

### 9.2 填写系统配置

左侧菜单 → **⚙️ 系统配置**，按分组填写：

| 分组           | 必填项                    | 说明            |
| ------------ | ---------------------- | ------------- |
| **🤖 机器人配置** | TG Bot Token、管理员 TG ID | @BotFather 获取 |
| **🎣 采集账号池** | api\_id、api\_hash、手机号  | 三组数量必须一致      |
| **🔍 搜索风控**  | 可选，用默认值即可              | 单次返回条数、每日搜索次数 |
| **💰 钱包与定价** | TronGrid API Key（推荐）   | USDT 充值对账用    |
| **⚙️ 系统路径**  | 可选，用默认值即可              | 数据库路径、会话目录    |

填完后点右下角 **💾 保存全部并生效**（即时生效，无需重启）。

### 9.3 启动 Bot 服务

```bash
# 启动服务
systemctl start tg-search-bot
systemctl start tg-search-admin

# 设置开机自启
systemctl enable tg-search-bot
systemctl enable tg-search-admin

# 检查状态
systemctl status tg-search-bot
systemctl status tg-search-admin
```

### 9.4 运行健康自检

```bash
cd /www/wwwroot/tg-search-bot
source venv/bin/activate
bash deploy/scripts/health_check.sh
```

***

## 10. 常见运维命令

### 服务管理

```bash
systemctl status  tg-search-bot   # 查看 Bot 状态
systemctl status  tg-search-admin # 查看 Admin 状态
systemctl restart tg-search-bot   # 重启 Bot（改代码后）
systemctl restart tg-search-admin # 重启 Admin
systemctl enable  tg-search-bot   # 开机自启
```

### 查看日志

```bash
tail -f /www/wwwroot/tg-search-bot/logs/stderr.log   # Bot 错误日志（排错重点）
tail -f /www/wwwroot/tg-search-bot/logs/admin_stderr.log  # Admin 错误日志
tail -f /www/wwwroot/tg-search-bot/logs/app.log       # 业务日志
journalctl -u tg-search-bot -f                        # 实时 Systemd 日志
journalctl -u tg-search-bot -n 100 --no-pager        # 最近 100 行
```

### 数据库备份

```bash
# 手动备份
cd /www/wwwroot/tg-search-bot && source venv/bin/activate
bash deploy/scripts/manual_backup.sh "备份说明"

# 查看备份列表
ls -lh data/backups/
```

### 账号健康检查

```bash
cd /www/wwwroot/tg-search-bot && source venv/bin/activate
bash deploy/scripts/health_check.sh
```

### 充值对账（手动触发）

```bash
cd /www/wwwroot/tg-search-bot && source venv/bin/activate
python -c "
import asyncio
from app.wallet.wallet_manager import WalletManager
async def main():
    wm = WalletManager()
    r = await wm.scan_deposits_and_credit()
    print(r)
asyncio.run(main())
"
```

### 定时任务（宝塔 → 计划任务）

脚本已自动注册，如需手动添加：

| 任务    | 周期        | 命令                                                                                                          |
| ----- | --------- | ----------------------------------------------------------------------------------------------------------- |
| 数据库备份 | 每天 03:00  | `cd /www/wwwroot/tg-search-bot && bash deploy/scripts/manual_backup.sh "定时备份" >> logs/cron_backup.log 2>&1` |
| 充值对账  | 每 5 分钟    | 同上方充值对账命令，追加到 `logs/cron_recharge.log`                                                                      |
| 日志清理  | 每周一 04:00 | `cd /www/wwwroot/tg-search-bot/logs && find . -name '*.log' -mtime +14 -delete`                             |

***

## 11. 排错手册

### Q1. 服务启动后立即停止

```bash
# 查看具体报错
journalctl -u tg-search-bot -n 100 --no-pager
tail -200 logs/stderr.log
```

常见原因：

* `ModuleNotFoundError` → 缺依赖，执行 `pip install -r requirements.txt`

* `ValueError: 配置校验失败` → 后台「系统配置」补全必填项

* `PermissionError` → `chown -R root:root /www/wwwroot/tg-search-bot`

### Q2. 机器人无响应

```bash
# 1. 检查日志
tail -100 logs/stderr.log

# 2. 检查账号健康
bash deploy/scripts/health_check.sh

# 3. 检查 Bot Token 有效性
curl -s "https://api.telegram.org/botYOUR_TOKEN/getMe" | head
```

### Q3. USDT 充值余额未到账

```bash
# 1. 手动执行对账
cd /www/wwwroot/tg-search-bot && source venv/bin/activate
python -c "
import asyncio
from app.wallet.wallet_manager import WalletManager
async def main():
    wm = WalletManager()
    r = await wm.scan_deposits_and_credit(verbose=True)
    print(r)
asyncio.run(main())
"

# 2. 检查 TronGrid API Key 是否已填写
# 3. 确认定时任务是否正常运行
crontab -l | grep tg-search
```

### Q4. 后台配置保存不生效

* 点 **🔄 重新加载** 刷新当前值

* 或执行 `systemctl restart tg-search-bot` 强制从 DB 重新加载

### Q5. 浏览器访问 /admin 黑屏

* 强制刷新：`Ctrl+Shift+R`

* 访问 `/admin?nocache=$(date +%s)`

* F12 → Console 查看 JS 报错

### Q6. 验证 HD 钱包主地址

```bash
cd /www/wwwroot/tg-search-bot && source venv/bin/activate
python -c "
import os; from dotenv import load_dotenv; load_dotenv()
mnemonic = os.getenv('HD_WALLET_MNEMONIC','').strip()
if not mnemonic: print('未配置助记词'); exit()
from app.wallet.wallet_manager import derive_tron_address
addr0 = derive_tron_address(mnemonic, 0)
print('✅ 主地址(索引0) =', addr0, '  请和 TronLink 核对')
"
```

***

## 12. 安全红线

> 🔴 **以下红线涉及资产安全，务必严格遵守！**

1. **HD\_WALLET\_MNEMONIC 只能手写在 .env 文件 + 纸上备份**

   * ❌ 绝不能录入后台系统配置页面

   * ❌ 绝不能截图 / 拍照

   * ❌ 绝不能通过微信、TG、邮件发送

   * ❌ 绝不能上传到云盘 / Git 仓库

2. **所有充值必须是 USDT-TRC20 网络！**

   * 用户充到 ERC20 / BEP20 等其他网络 = 永久丢失

3. **助记词索引 0 = 运营主地址**（归集用）

   * 用户充值地址从索引 1、2、3... 自动派生

4. **SESSION\_SECRET / CRYPTO\_SECRET 一旦生成不要更改**

   * 更改会导致所有加密配置解密失败

5. **定期离线备份 data/tg\_search.db 和 data/sessions/\*.session**

   * 丢失 = 小号登录态失效 + 用户数据全没

6. **.env 文件权限设为 600**

   ```bash
   chmod 600 /www/wwwroot/tg-search-bot/.env
   ```

***

## 附录：版本升级步骤

```bash
# ① 备份（重要！）
cd /www/wwwroot/tg-search-bot
cp data/tg_search.db data/tg_search.db.bak.$(date +%Y%m%d)
cp .env .env.bak.$(date +%Y%m%d)

# ② 停服务
systemctl stop tg-search-bot
systemctl stop tg-search-admin

# ③ 替换源码（保留 .env / data / logs / venv）
unzip -o tg-search-bot-v1.0.12-*.zip -d /tmp/newver
rsync -av \
  --exclude='.env' \
  --exclude='data/' \
  --exclude='logs/' \
  --exclude='venv/' \
  /tmp/newver/tg-search-bot/ /www/wwwroot/tg-search-bot/

# ④ 更新依赖（如 requirements.txt 有变化）
cd /www/wwwroot/tg-search-bot && source venv/bin/activate
pip install -r requirements.txt -i https://pypi.tuna.tsinghua.edu.cn/simple

# ⑤ 启动
systemctl daemon-reload
systemctl start tg-search-bot
systemctl start tg-search-admin

# ⑥ 验证
bash deploy/scripts/health_check.sh
```

