# TG Search Bot 交接文档 v1.0.27

> 生成时间：2026-09-04  
> 项目仓库：https://github.com/tw-search-bot/tg-search-bot  
> 服务器地址：http://jsou.tgjsbot.kdns.fr / SSH: root@186.244.251.12  
> 维护人：AI高级软件开发工程师（Agnes）

---

## 一、项目概述

TG Search Bot 是一个基于 Telegram 的关键词搜索机器人系统，同时提供 Web 管理后台和演示页面。  
系统采用 **双进程架构**：

| 进程 | 端口 | 职责 |
|------|------|------|
| **FastAPI Server** | 8001 | REST API、Web 后台、演示页、Bot 命令中枢 |
| **Telegram Bot** | 8000 | Telegram 长轮询、消息接收、Bot 命令执行 |

核心依赖：`telethon`（电报爬虫）、`python-telegram-bot`（Bot）、`fastapi`（Web）、`jellyfish`（搜索模糊匹配）、`jieba`（中文分词）

---

## 二、已完成功能

### 2.1 核心 Bot 功能
- ✅ `/start` — 欢迎页（HTML格式 + 热搜词按钮 + 热门广告）
- ✅ `/help` — 命令总览
- ✅ `/stats` — 数据统计（频道数、消息数）
- ✅ `/wallet` — 钱包余额查询
- ✅ `/recharge` / `/checkrecharge` — USDT充值流程
- ✅ `/advertise` / `/createad` / `/myads` / `/adtemplates` / `/adstats` — 广告管理全流程
- ✅ `/add @channel` — 添加采集频道
- ✅ `/channels` / `/ads` — 频道/广告列表
- ✅ AI 搜索（`/ai` 命令）— 调用 DeepSeek API 扩展关键词
- ✅ **热搜词按钮点击** — `kw_callback_handler` 处理 `__kw__` 前缀回调，触发快捷搜索
- ✅ **每日搜索限制** — 免费用户每日 5 次，VIP 无限

### 2.2 Web 管理后台
- ✅ 登录认证（admin/demo123456，已重置）
- ✅ 仪表盘（统计概览）
- ✅ 爬虫账号管理（增删改查、健康检测、代理测试）
- ✅ 频道管理（列表、统计）
- ✅ 广告管理（列表、创建、状态切换）
- ✅ 热搜词管理（分类、排序、启用/禁用）
- ✅ 系统设置（配置编辑、一键备份、版本更新检查）
- ✅ 诊断工具（AI状态、搜索测试、错误日志导出）
- ✅ 用户管理（用户列表、充值记录、余额调整）

### 2.3 搜索与爬虫
- ✅ Telethon 账号池（多账号轮换，健康检测）
- ✅ 频道消息爬取（自动 + 手动触发）
- ✅ 关键词索引（SQLite + jellyfish 模糊匹配）
- ✅ AI 搜索扩展（DeepSeek API）
- ✅ 搜索结果去重与排序

### 2.4 钱包与支付
- ✅ TRC-20 USDT 钱包生成（hdwallet）
- ✅ 充值地址管理
- ✅ 充值确认（手动 + 自动检测）
- ✅ 搜索扣费（每次搜索扣除设定金额）
- ✅ VIP 权限控制

### 2.5 演示页面（v1.0.27 新增）
- ✅ 16:9 竖屏适配
- ✅ 模块化卡片（搜索/广告/统计）
- ✅ 卡片拖拽与缩放（interact.js）
- ✅ 模块化排序（API: `/api/demo/config`）
- ✅ **一键推送到机器人**（API: `/api/bot/push_demo`）
- ✅ 热搜词一行显示，`|` 分隔
- ✅ 热门广告单行卡片，点击加入直达频道

### 2.6 CI/CD（v1.0.27 新增）
- ✅ GitHub Actions CI（lint + test + build）
- ✅ 自动 Release 创建（tag v*）
- ✅ 源码包上传 Release

---

## 三、未完成 / 待规划功能

| 模块 | 功能 | 优先级 | 说明 |
|------|------|--------|------|
| Bot | 消息撤回/编辑 | 中 | 支持编辑上次搜索回复 |
| Bot | 图片/卡片推送 | 低 | 富媒体搜索结果展示 |
| 演示页 | 配置持久化 | 中 | demo_config 写入 DB，重启不丢失 |
| 演示页 | 多用户切换 | 低 | 模拟不同用户视角 |
| 爬虫 | 自动重试机制 | 高 | 封号自动换号 |
| 钱包 | 自动充值检测 | 高 | 链上监听 + 自动确认 |
| 管理 | 操作日志审计 | 中 | 管理员操作记录 |
| 管理 | 多语言支持 | 低 | i18n 国际化 |
| 安全 | 请求限流 | 高 | API 速率限制 |
| 监控 | Prometheus 指标 | 中 | 接入 Grafana 面板 |

---

## 四、目录结构

```
tg-search-bot/
├── app/                      # 核心业务模块
│   ├── admin/               # 后台管理逻辑
│   │   ├── backup_manager.py
│   │   ├── system_settings_manager.py
│   │   └── version_manager.py
│   ├── advertising/         # 广告模块
│   │   └── ad_manager.py
│   ├── ai/                  # AI 搜索
│   │   ├── model_service.py
│   │   └── settings_manager.py
│   ├── bot/                 # Telegram Bot
│   │   └── handlers.py      # 命令处理器（含 kw_callback_handler）
│   ├── crawler/             # 爬虫
│   │   ├── account_pool.py
│   │   ├── channel_crawler.py
│   │   └── message_listener.py
│   ├── search/              # 搜索索引
│   │   └── indexer.py
│   ├── wallet/              # 钱包
│   │   └── wallet_manager.py
│   ├── config.py
│   └── database.py
├── deploy/                  # 部署脚本
│   ├── auto_deploy.sh
│   ├── baota_install.sh
│   └── docker-entrypoint.sh
├── templates/               # Web 页面模板（内嵌 HTML）
├── static/                  # 静态资源
│   ├── css/
│   │   └── demo.css         # 演示页样式
│   └── js/
│       └── demo.js          # 演示页交互
├── server.py                # FastAPI 主服务
├── main.py                  # Bot 启动入口
├── requirements.txt
├── .env.example
├── Dockerfile
├── docker-compose.yml
├── .github/workflows/ci.yaml # CI/CD 配置
├── 交接文档_v1.0.27.md      # 本文件
└── pack_to_desktop.py       # 打包脚本
```

---

## 五、关键 API 接口

### 5.1 Bot 命令接口
| 接口 | 方法 | 说明 |
|------|------|------|
| `/api/bot/command` | POST | Bot 命令中枢，Web 和 Bot 共用 |
| `/api/bot/ad_templates` | GET | 广告模板列表 |
| `/api/bot/create_ad` | POST | 创建广告 |
| `/api/bot/myad_status` | POST | 更新广告状态 |
| `/api/bot/myad_delete` | POST | 删除广告 |
| `/api/bot/myad_update` | POST | 更新广告 |
| `/api/bot/ai/usage` | GET | AI 用量统计 |

### 5.2 管理后台接口
| 接口 | 方法 | 说明 |
|------|------|------|
| `/admin` | GET | 管理后台首页 |
| `/api/admin/check_auth` | GET | 检查登录状态 |
| `/api/admin/login` | POST | 登录 |
| `/api/admin/logout` | POST | 登出 |
| `/api/admin/crawler_accounts` | GET/POST | 账号管理 |
| `/api/admin/channels` | GET | 频道列表 |
| `/api/admin/hot_keywords` | GET/POST | 热搜词管理 |
| `/api/dashboard` | GET | 仪表盘数据 |
| `/api/diagnostic` | GET | 诊断信息 |

### 5.3 演示页接口（v1.0.27 新增）
| 接口 | 方法 | 说明 |
|------|------|------|
| `/demo` | GET | 演示页面入口 |
| `/api/demo/config` | GET/POST | 演示配置读写 |
| `/api/bot/push_demo` | POST | 一键推送到 Bot |

---

## 六、数据库表结构

| 表名 | 说明 |
|------|------|
| `users` | 用户表（id, tg_id, username, balance, is_vip, daily_count） |
| `channels` | 频道表（id, username, title, crawl_status, is_featured） |
| `messages` | 消息索引表（id, channel_id, message_id, text, keywords, timestamp） |
| `ads` | 广告表（id, user_id, title, description, target_url, status, spend） |
| `hot_keywords` | 热搜词表（id, keyword, category, display_order, is_active） |
| `transactions` | 充值记录（id, user_id, amount, status, tx_hash, created_at） |
| `system_settings` | 系统设置（key, value） |
| `crawler_accounts` | 爬虫账号（id, phone, session_string, is_active, last_use） |

---

## 七、环境变量配置（`.env`）

```env
# Bot 配置
BOT_TOKEN=123456:ABC-xxxxxxx    # Telegram Bot Token
BOT_CHAT_ID=987654321           # 管理员 Chat ID（用于推送通知）

# 数据库
DB_PATH=data/tg_search.db

# AI 搜索
DEEPSEEK_API_KEY=sk-xxxx        # DeepSeek API Key
DEEPSEEK_MODEL=deepseek-chat

# 搜索配置
FREE_SEARCH_DAILY_LIMIT=5       # 免费用户每日搜索次数
VIP_SEARCH_DAILY_LIMIT=0        # 0 = 无限制
SEARCH_COST_PER_QUERY=0.1       # 每次搜索扣费

# 服务器
HOST=0.0.0.0
PORT=8001

# 广告
FEATURED_AD_LIMIT=3             # 首页展示广告数量
HOT_KEYWORD_PER_CATEGORY_LIMIT=6
```

---

## 八、部署与维护

### 8.1 快速部署（宝塔）
```bash
# 1. 克隆仓库
git clone https://github.com/tw-search-bot/tg-search-bot.git /www/wwwroot/tg-search-bot
cd /www/wwwroot/tg-search-bot

# 2. 配置环境变量
cp .env.example .env
vi .env  # 填入 BOT_TOKEN、DEEPSEEK_API_KEY 等

# 3. 安装依赖
pip install -r requirements.txt -i https://pypi.tuna.tsinghua.edu.cn/simple

# 4. 创建 systemd 服务
cp deploy/tg-search-bot.service /etc/systemd/system/
systemctl daemon-reload
systemctl enable tg-search-bot
systemctl start tg-search-bot

# 5. 验证
systemctl status tg-search-bot
curl http://localhost:8001/health
```

### 8.2 手动部署（Docker）
```bash
docker-compose up -d
```

### 8.3 常用命令
```bash
# 查看日志
journalctl -u tg-search-bot -f

# 重启服务
systemctl restart tg-search-bot

# 备份数据库
cp data/tg_search.db data/tg_search_backup_$(date +%Y%m%d).db

# 检查端口
ss -tlnp | grep -E '8000|8001'
```

---

## 九、GitHub 更新规范

### 9.1 分支策略
- `main` — 生产稳定版本
- `develop` — 开发分支
- `feature/*` — 功能分支（如 `feature/demo-page`）
- `hotfix/*` — 紧急修复分支

### 9.2 提交规范
```
feat: 新增演示页面拖拽缩放功能
fix: 修复 Bot 连接 407 代理错误
docs: 更新交接文档
chore: 更新依赖版本
```

### 9.3 版本发布流程
```bash
# 1. 在 develop 分支完成开发
git checkout develop
git pull origin develop

# 2. 创建 release 分支
git checkout -b release/v1.0.28

# 3. 合并 develop
git merge develop

# 4. 打标签
git tag -a v1.0.28 -m "Release v1.0.28: 新增 AI 搜索扩展、优化热搜词展示"

# 5. 推送
git push origin release/v1.0.28
git push origin v1.0.28

# 6. GitHub Actions 自动创建 Release
```

### 9.4 交接时如何不影响 GitHub 更新
1. **本地修改前**：先 `git stash` 保存未提交修改
2. **pull 最新代码**：`git pull --rebase origin main`
3. **创建新分支**：`git checkout -b feature/xxx`
4. **开发完成后**：提交到 `develop`，PR 到 `main`
5. **标签推送**：合并后打 tag 并推送，触发 CI/CD
6. **本交接文档**：始终放在 `main` 分支根目录，新成员可直接查阅

---

## 十、已知问题与注意事项

| 问题 | 说明 | 状态 |
|------|------|------|
| 407 代理错误 | 服务器网络需要代理才能访问 Telegram API | ⚠️ 需配置代理或直连 |
| Bot 崩溃重启 | 网络波动时 Bot 会 5 次重连后退出 | ⚠️ systemd 已配置自动重启 |
| 热搜词按钮不生效 | 前端 onclick 依赖 JS，Markdown 模式下失效 | ✅ 已修复（handlers.py start_command 改 HTML 模式） |
| 演示配置不持久化 | demo_config 仅存内存 | 📋 待实现 DB 持久化 |
| GitHub Actions 未配置 Secret | PYPI_API_TOKEN 未设置 | 📋 可选功能 |

---

## 十一、联系方式

- **GitHub**：https://github.com/tw-search-bot/tg-search-bot
- **服务器**：root@186.244.251.12（密码见运维文档）
- **Web 后台**：http://jsou.tgjsbot.kdns.fr/admin
- **演示页**：http://jsou.tgjsbot.kdns.fr/demo

---

> 本文档由 AI 自动生成，如有疑问请联系项目负责人。
> 最后更新：2026-09-04 | 版本：v1.0.27
